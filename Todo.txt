issue: scrolling right messes up display

align panes right

separate stream pane lines
	pane with custom renderer

give size of panes
	for ansi_poupou
		in columns or percentage

fixed header lines
	top status, multi line
		read from stream
	per pane
		display stream name
		
watch files
	via external app that sends signal instead for internal implementation
	reload specific file
		what when the stream is not from a file?
			reload
			implies signals are for column not file names

search
	highlight

filter
	limit number of lines
	& filter

select pane

cursor
	space to add line numbers
	separate top_line

commands
	insert/delete line in a stream, single or all panes
	vertical alignment

external commands
	on what?

